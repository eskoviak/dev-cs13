<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>RWfB_same_as_Physical_Address__c</fullName>
    <defaultValue>false</defaultValue>
    <description>Used to identify if the RWfB Address is the same (true) or different (false) than the IA Physical Location fields.</description>
    <externalId>false</externalId>
    <inlineHelpText>Used to identify if the RWfB Address is the same (true) or different (false) than the IA Physical Location fields.</inlineHelpText>
    <label>RWfB same as Physical Address</label>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
